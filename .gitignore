# Rust
/target/
Cargo.lock

# Data (parquet segments are large — only track rule YAML configs)
/data/cache/
/data/segments/
/data/sqs/
/data/catalog/
/data/embeddings/
/data/agent-sessions/

# Node / TypeScript
node_modules/
.next/
dist/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Environment — never commit secrets
.env
.env.local
!.env.example

# Claude Code (local/ephemeral state)
.claude/*.local.md
.claude/*.local.json
.claude/verrueckt-metadata.json
.claude/insights
.claude/memory/

# Python
__pycache__/
*.pyc
.venv/

# Beads (local Dolt DB — tracked by its own .gitignore, but belt-and-suspenders)
.beads/dolt/
.beads/dolt-access.lock
.beads/.local_version
.beads/last-touched

data/.conn_key
data/athena-connections.json
data/athena-query-log-ms-athena.json
data/connections.json