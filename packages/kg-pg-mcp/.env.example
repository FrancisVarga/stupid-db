# PostgreSQL connection (required)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/knowledge_graph

# OpenAI API key (required for embedding)
OPENAI_API_KEY=sk-...

# Embedding model (optional, defaults to text-embedding-3-small)
# Options: text-embedding-3-small (1536 dims), text-embedding-3-large (3072 dims)
EMBEDDING_MODEL=text-embedding-3-large

# Chunking settings (optional)
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Default namespace (optional)
DEFAULT_NAMESPACE=default

# Server transport: stdio | sse (optional, defaults to stdio)
TRANSPORT=stdio

# SSE server settings (only used when TRANSPORT=sse)
HOST=0.0.0.0
PORT=8100
