services:
  kg-pg-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "12312:12312"
    env_file:
      - .env
    environment:
      KG_TRANSPORT: sse
      KG_HOST: 0.0.0.0
      KG_PORT: 12312
    restart: unless-stopped
